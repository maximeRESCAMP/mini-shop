{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    {% embed 'partials/_form.html.twig' with {
        name: 'un compte',
        is_update: false,
    } %}
        {% block form %}


            {{ form_start(form) }}

            <div class="my-custom-class-for-errors mb-3">
                {{ form_errors(form) }}
            </div>

            <fieldset class="border rounded p-4 mb-4">
                <legend class="w-auto px-2 fw-bold">Informations personnelles</legend>
                <div class="row g-3">
                    <div class="col-md-6">
                        {{ form_row(form.email) }}
                    </div>
                    <div class="col-md-6">
                        {{ form_row(form.phone) }}
                    </div>
                    <div class="col-md-6">
                        {{ form_row(form.password.first) }}
                    </div>
                    <div class="col-md-6">
                        {{ form_row(form.password.second) }}
                    </div>


                    <div class="col-md-6">
                        {{ form_row(form.firstName) }}
                    </div>

                    <div class="col-md-6">
                        {{ form_row(form.lastName) }}
                    </div>


                </div>
            </fieldset>

            <fieldset class="border rounded p-4 mb-4">
                <legend class="w-auto px-2 fw-bold">Adresse de livraison</legend>

                {% for deliveryAddresses in form.addresses %}
                    {{ form_row(deliveryAddresses) }}
                {% endfor %}

            </fieldset>

            <div>
                {{ form_row(form.save) }}
            </div>
            {{ form_end(form) }}
        {% endblock %}
    {% endembed %}
{% endblock %}
